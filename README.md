# 2PGameArcade

2-player pixel arcade game built with C++ and raylib.

## Build

Use the default VS Code build task in this repository.

The project uses a slimmed-down custom raylib build for Windows to keep the final executable size low.
